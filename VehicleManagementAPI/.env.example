# Environment Variables Template
# Copy this file to .env (for local development) or configure in Railway Dashboard

# Database Connection
# Local development: Use your local MySQL connection string
# Railway: DATABASE_URL is automatically provided by Railway's MySQL service
DATABASE_URL=mysql://user:password@localhost:3306/vehicle_maintenance

# JWT Configuration
# IMPORTANT: Change this to a strong, unique secret key in production!
JWT_SECRET_KEY=YourSuperSecretKeyForJWTTokenGeneration123!ChangeThisInProduction
JWT_ISSUER=VehicleManagementAPI
JWT_AUDIENCE=VehicleManagementClient
JWT_EXPIRATION_MINUTES=1440

# ASP.NET Core Environment
ASPNETCORE_ENVIRONMENT=Development
# For Railway, set to: Production

# Server URLs
# Local: Use default ports
# Railway: Set to http://0.0.0.0:$PORT
ASPNETCORE_URLS=http://localhost:5000;https://localhost:5001

# CORS - Allowed Origins (comma-separated)
# Add your frontend URLs here
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Railway-specific (set in Railway Dashboard)
# PORT=8080 (Railway sets this automatically)
